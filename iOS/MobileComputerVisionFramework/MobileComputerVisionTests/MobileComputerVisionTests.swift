//
//  MobileComputerVisionTests.swift
//  MobileComputerVisionTests
//
//  Created by Strategic Impact Solutions 1 on 26/04/18.
//  Copyright © 2018 App Builders. All rights reserved.
//

import XCTest

class MobileComputerVisionTests: XCTestCase {
    
    override func setUp() {
        
        super.setUp()
    }
    
    override func tearDown() {
        // Put teardown code here. This method is called after the invocation of each test method in the class.
        super.tearDown()
    }
    
    func testExample() {
        
        
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct results.
        
        /*let vc = UIViewController();
        let bundle = Bundle.init(for: TextDetector.self);
        let image = UIImage(named: "ocr", in: bundle, compatibleWith: nil);
        
        let detector = TextDetector.init(controller: vc);
        let text = detector.startWithImage(image: image).getText();
        print("Readed text :: ", text);*/
    }
    
    func testPerformanceExample() {
        // This is an example of a performance test case.
        self.measure {
            // Put the code you want to measure the time of here.
        }
    }
    
}
